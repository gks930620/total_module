# 사용자가 해야 할 일

> 갱신: 2026-07-08 (게이트웨이 청산 코드 작업 완료 — `remove-gateway` 브랜치)
> 방향 기준: [`설계/아키텍처_방향결정.md`](../설계/아키텍처_방향결정.md) §4

---

## ✅ 완료된 것

- 문제점 전체 수정 + master 병합·push (`6806fa5`)
- **청산 코드 작업 (`remove-gateway` 브랜치)**: 앱 base URL → businesscardqr 자기 도메인, dist_api_gateway 모듈 삭제,
  빌드 설정 정리, 문서/다이어그램 전면 갱신. 테스트 통과 (businesscard_qr 22/22 + bootJarAll)

## 🔴 지금 순서대로 할 일

| # | 할 일 | 메모 |
|---|-------|------|
| 1 | Railway → businesscard_qr → Variables: **`APP_PUBLIC_BASE_URL=https://businesscardqr-production.up.railway.app`** 추가 | master push 전에 미리 넣어도 무해 |
| 2 | `remove-gateway` 브랜치에서 앱 빌드: `cd businesscard_qr_app` → `flutter pub get` → `flutter build apk` → 설치 | 이 APK는 게이트웨이 안 거침 |
| 3 | **직접 연결 전 기능 확인**: 카카오 로그인 / 명함 생성(이미지 포함) / QR 스캔(다른 폰) / 이미지 표시 | QR·이미지 URL이 `businesscardqr-production...`으로 시작하는지 |
| 4 | 3이 전부 정상이면: **master 병합·push** (Claude에게 "병합해줘") | push 후 businesscard_qr 재배포됨 |
| 5 | Railway에서 **dist_api_gateway 서비스 삭제** | 비용 절감. 3 확인 전엔 삭제 금지 |

> ⚠️ master push 전까지 운영(기존 앱↔게이트웨이)은 그대로 동작하므로 서두를 필요 없음.
> 문제 발견 시 브랜치만 버리면 원상복구.

## 🟡 선택

| 환경변수 (businesscard_qr) | 값 | 효과 |
|----------|-----|------|
| `APP_KAKAO_EXPECTED_APP_ID` | 카카오 앱 ID (숫자) | 다른 카카오 앱 토큰 차단. 미설정 시 기존과 동일 |
| `APP_JWT_REFRESH_TOKEN_VALIDITY_SECONDS` | 초 (기본 1209600=14일) | 리프레시 토큰 유효기간 |

## ✅ 청산 완료 후 5분 체크리스트

1. [ ] `https://businesscardqr-production.up.railway.app/healthz` → `status: UP`
2. [ ] 명함 생성 → QR·이미지 URL 도메인 확인
3. [ ] 재배포 후 데이터 보존
4. [ ] 30분+ 방치 후 첫 실행: 무한 로딩 없이 재시도 UX 동작
5. [ ] businesscard_qr 로그에 APP_PUBLIC_BASE_URL WARN 사라짐

## 📌 새 프로젝트(A) 가져올 때

- 로컬에서 완성 → 폴더 투하 → Claude에게 "프로젝트 A 편입해줘"
- 절차: [`설계/아키텍처_방향결정.md`](../설계/아키텍처_방향결정.md) §3, 웹이면 [`설계/웹프로젝트_통합_설계기준.md`](../설계/웹프로젝트_통합_설계기준.md)
- 사용자 준비물: DB(기존 mysql 공유 or 새 DB), 시크릿 값, Railway 서비스 생성(+ Add)
