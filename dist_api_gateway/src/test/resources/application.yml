server:
  port: 0
  forward-headers-strategy: framework

spring:
  servlet:
    multipart:
      # 운영과 동일하게 multipart 파서를 꺼서 raw 바이트 프록시 동작을 검증한다.
      enabled: false

app:
  gateway:
    target-base-url: http://upstream.test
