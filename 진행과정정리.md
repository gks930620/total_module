# 진행과정정리

## 2026-04-23

### Git/브랜치 정리
- `master`와 `origin/master`를 동일 커밋(`39fec97`)으로 정렬
- `dist_api_gateway` 관련 stash 제거
- 작업 브랜치 `배포설정-작업` 생성

### 배포 구조 정리
- 루트를 멀티모듈(`businesscard_qr`, `dist_api_gateway`)로 구성
- 루트 `Dockerfile`/`railway.toml` 추가
- `APP_MODULE` 환경변수로 실행 모듈 선택 가능하도록 설정

### gateway 최소화
- `dist_api_gateway`를 gateway 전용으로 축소
- `/api/**`, `/uploads/**`, `/swagger-ui/**`, `/v3/api-docs/**` 프록시 컨트롤러 추가
- 기존 `community/file/jwt/stomp` 등 gateway 무관 Java 코드 삭제

### businesscard_qr 연동 보정
- `businesscard_qr` 기본 포트를 `8081`로 조정 (`PORT` 우선 반영)
- `forward-headers-strategy: framework` 적용
- Flutter 앱에서 `GATEWAY_BASE_URL` 우선 사용하도록 변경

### 검증
- `:businesscard_qr:bootJar` 성공
- `:dist_api_gateway:bootJar` 성공
- `:businesscard_qr:test` 성공
- `bootJarAll` 성공

### 보완 메모
- `rg`(ripgrep) 설치 수행(winget)
- 현재 터미널 세션은 PATH 갱신 전이라 새 터미널에서 `rg --version` 확인 필요
