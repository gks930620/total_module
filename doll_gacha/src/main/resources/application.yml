dotenv:
  enabled: true  # .env 파일 로드. KAKAO_CLIENT_ID 등을 직접 넣으려면 없어도 됨.

# =========================================================
# 공통 설정 + 로컬(기본 프로파일) 설정
#  - 로컬: H2 인메모리 (서버 켤 때마다 새로 서고 시드 재로드 → 데이터 비영속, 별도 DB 설치 불필요)
#  - 배포: application-prod.yml (Railway MySQL, 데이터 영속). SPRING_PROFILES_ACTIVE=prod 로 활성화
# =========================================================
# Railway 등 리버스 프록시(HTTPS 종료) 뒤에서 X-Forwarded-* 헤더를 신뢰하여
# OAuth2 redirect_uri, 절대 URL 생성 시 내부 호스트 대신 공개 https 도메인을 사용
server:
  forward-headers-strategy: framework

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  servlet:
    multipart:
      max-file-size: 10MB      # 단일 파일 최대 크기
      max-request-size: 50MB   # 전체 요청 최대 크기

  # ----- 로컬 DB: H2 인메모리 (MySQL 호환) -----
  #  서버 켤 때마다 새로 생성되고 시드가 매번 재로드된다 → 로컬 데이터는 남지 않는다(휘발).
  #  깨진 데이터/스키마 잔재로 인한 문제를 원천 차단. (운영은 application-prod.yml = 영속 MySQL)
  datasource:
    url: jdbc:h2:mem:doll_gacha;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true   # http://localhost:8080/h2-console
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create           # 인메모리: 켤 때마다 스키마 새로 생성 (데이터 비영속)
    defer-datasource-initialization: true  # DDL 후 data-*.sql 실행
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: false

  sql:
    init:
      mode: always
      encoding: UTF-8            # 시드 .sql은 UTF-8. 미설정 시 JDK17+한글 Windows가 CP949로 읽어 한글이 깨진다.
      # (인메모리 + ddl-auto: create 라 매번 빈 스키마에 시드가 새로 들어감 → 중복 INSERT 없음)
      data-locations:
        - classpath:data-users.sql
        - classpath:data-dollshop.sql
        - classpath:data-files.sql
        - classpath:data-review.sql
        - classpath:data-community.sql
        - classpath:data-comment.sql

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  # ----- OAuth2 (공통: redirect-uri 는 APP_BASE_URL 로 로컬/운영 분기) -----
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            redirect-uri: ${APP_BASE_URL:http://localhost:8080}/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            scope:
              - profile_nickname
              - account_email
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: ${APP_BASE_URL:http://localhost:8080}/login/oauth2/code/google
            scope:
              - profile
              - email
            authorization-grant-type: authorization_code
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

jwt:
  secret: ${JWT_SECRET_KEY}
  expiration_access: 1800000    # 30분
  expiration_refresh: 14400000  # 4시간

# 쿠키 보안 설정 (로컬: HTTP 이므로 false)
app:
  cookie:
    secure: false
  oauth2:
    # 앱(target=app) OAuth2 완료 후 토큰 전달 리다이렉트 베이스 (로컬: Android 에뮬레이터)
    app-redirect-base: ${APP_OAUTH2_REDIRECT_BASE:http://10.0.2.2:8080}

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: when_authorized

# Swagger
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs

# (파일은 DB(stored_files)에 저장 — 디스크 업로드 경로 불필요)
